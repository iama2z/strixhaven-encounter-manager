// widget_test.dart — placeholder smoke test.
//
// Full widget tests require a Firebase test environment. The encounter model
// unit tests live in encounter_model_test.dart.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — no-op smoke test', () {
    // Add widget tests here once a Firebase emulator / mock is available.
    expect(true, isTrue);
  });
}
